# Work Log — 2026-04-12

---

## Summary

7 个后端模块完成从审计到重写的完整工作流。新增 146 个单元测试，修复 67+ 个代码问题。涉及 35 个文件，+7454/-1152 行。

**Commit:** `c4d272a` feat: 7-module backend overhaul

---

## 工作流程

每个模块严格遵循 9 步工作流：
1. 写 PRD（问题分析 + 解决方案 + TODO）
2. 分析现有代码（逐行审计）
3. 写设计文档（逐函数改动方案）
4. 讨论确认（与用户对齐设计）
5. 写单元测试（先测试后实现）
6. 实现代码
7. 运行测试 + 全量回归
8. 更新 ui-guide.html（双语）
9. 等用户确认后提交

---

## 模块完成清单

### 1. Monitor Center（monitor.py — 全面重写）
- **删除** 所有 CloudWatch 代码（3 个函数）
- **删除** 所有 SSM takeover 代码 → DynamoDB with TTL
- **新增** get_action_items(), get_system_status(), get_event_stream(), get_agent_activity()
- **新增** refresh_all_agents()
- Alert rules: 只保留真实数据支撑的规则
- Quality scores: 真实 _calculate_agent_quality()
- Plan E: 真实 PII 模式匹配（SSN, credit card, credentials）
- **22 个测试**

### 2. Audit Center（audit.py — 全面重写）
- Manager scope: actorId（非 actorName）
- 时间范围过滤（since/before 参数）
- Scan threshold 修复（>=2 → >=3）
- 硬编码 ORG#acme → db.ORG_PK
- **新增** 2 个扫描检查：pending reviews >24h, permission denial spike
- **新增** Review Queue（get/approve/reject）
- **新增** AI Deep Analyze（Bedrock Converse）
- **新增** Compliance Stats（daily breakdown, SOUL compliance rate）
- **14 个测试**

### 3. Usage & Cost（usage.py — 全面重写）
- **删除** ChatGPT 对比（$0.83）
- 分层预算：resolve_budget(emp_id, dept_name)，3 级继承
- 预算预测：7 天平均 × 30（非简单 used × 30）
- 未知模型保持 "unknown"（不默认 Nova Lite）
- by-model 5 分钟缓存
- **新增** my-budget（员工门户）, department-budget（经理视图）
- Budget 更新创建 AUDIT# 条目
- **15 个测试**

### 4. Settings（settings.py — 扩展）
- 7 个端点增加 require_role
- 9 个配置变更增加 AUDIT# 审计
- Model 变更: bump_config_version + force refresh
- **新增** platform-access（SSM 命令预构建）
- **新增** platform-logs（journalctl）
- **新增** restart-service（systemctl restart）
- **新增** admin-assistant/history（GET/DELETE）
- Admin assistant config: systemPrompt + maxHistoryTurns + maxTokens
- **25 个测试**

### 5. Playground（playground.py — 全面重写）
- **删除** _POS_TOOLS 硬编码 → DynamoDB POS#.toolAllowlist
- **删除** 关键词匹配 → Bedrock Converse with real SOUL
- **新增** get_pipeline_config() — 完整运行时配置
- **新增** get_playground_events() — AUDIT# 交互事件
- Admin 助手: 委托 admin_ai.py（无 subprocess）
- **11 个测试**

### 6. IM Channels（admin_im.py + bindings.py + portal.py）
- **修复** get_im_channels() SSM 分页 bug（MaxResults=10 无循环）→ DynamoDB
- **修复** im_binding_check() 硬编码 ORG#acme → db.ORG_PK
- **删除** _list_user_mappings() 重复（admin_im.py + bindings.py 各一份）
- **删除** resolve_route() 共享 Agent 路由代码
- **增加** bindings CRUD 认证（require_role）
- **修复** portal.py SSM 扫描（_find_channel_user_id, _list_user_mappings_for_employee）→ DynamoDB
- **新增** set_im_bot_info() 审计 + Pydantic 校验
- **新增** IM channel health API
- **新增** Enrollment stats API
- **新增** Batch unbind API
- **16 个测试**

### 7. Admin AI（admin_ai.py — 修正）
- 修复 chatgptEquivalent 引用 → 删除
- 修复 _get_agent_usage_today → _get_agent_usage_recent
- 增加 AUDIT# 审计追踪

### 8. Agent Container（跨模块）
- permissions.py: _log_permission_denied() 写 DynamoDB AUDIT#
- server.py: MODEL_PRICING dict 模型感知计费

---

## 文档产出

| 类型 | 文件数 |
|------|--------|
| PRD | 7（monitor, audit, usage, settings, playground, im-channels, + 之前的） |
| Design Doc | 7 |
| Unit Tests | 10 个文件, 146 个测试 |
| ui-guide.html | 每个模块的审计发现 + 修复记录（双语） |

---

## 安全修复汇总

| 类别 | 数量 | 示例 |
|------|------|------|
| 认证缺口 | 10+ | settings 7 个端点, bindings 3 个端点 |
| 审计缺口 | 12+ | settings 9 个, bot-info 1 个, batch-unbind 1 个 |
| 硬编码 | 4 | ORG#acme × 2, _POS_TOOLS, ChatGPT comparison |
| SSM 分页 bug | 3 | get_im_channels, _find_channel_user_id, _list_user_mappings_for_employee |
| 数据重复 | 2 | _list_user_mappings × 2 份 |
| 死代码 | 2 | shared agent routing, keyword matching simulate |

---

## 未完成 / 下次会话

- [ ] **Skill Market 模块** — 需要单独会话（与员工门户关联）
- [ ] **Portal / Employee 模块** — PRD 未写
- [ ] **Fargate Always-On IM 集成** — 半完成，需重新设计（一套前端，不同 API）
- [ ] **前端重写** — 所有 10 个模块的 React 页面需匹配后端改动
- [ ] **Docker 镜像重建** — workspace_assembler.py, server.py, permissions.py
- [ ] **DynamoDB TTL** — takeoverTTL 属性各环境启用
- [ ] **生产部署** — 最新代码部署到东京环境
- [ ] **端到端测试** — SOUL 加载, 监控触发, playground 验证, 各页面数据刷新
